[project]
name = "genai-class"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "jupyter>=1.1.1",
    "pydantic>=2.11.7",
    "uvicorn>=0.35.0",
]
